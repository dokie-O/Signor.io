root = "."
tmp_dir = "tmp"

[build]
    bin = "main"
    cmd = "go build -o {{.Output}} {{.Input}}"
    exclude = ["tmp/*", "client/*"]
    include = ["**/*.go"]
    ignore = ["tmp/*"]

# based on "https://youtu.be/lNd7XlXwlho?t=909"